{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/shakacode/agent-coordination/blob/main/schema/state/v1/capacity-reservation/inbox.schema.json",
  "title": "Agent Coordination Inbox v1",
  "description": "Authoritative enabled or disabled planning inbox bound to one capacity profile.",
  "x-contract-version": 1,
  "x-record-family": "inbox",
  "x-logical-key": ["workspace", "inbox_id"],
  "x-storage-key": {
    "template": "inboxes/{workspace}/{inbox_id}.json",
    "segment_encoding": "UTF-8 bytes; leave ASCII alphanumeric, underscore, and hyphen; percent-encode every other byte with uppercase hexadecimal"
  },
  "$ref": "#/$defs/inbox",
  "$defs": {
    "workspace": {
      "type": "string",
      "minLength": 1,
      "default": "default"
    },
    "canonical_id": {
      "type": "string",
      "minLength": 1,
      "maxLength": 128,
      "pattern": "^[a-z0-9](?:[a-z0-9._-]{0,126}[a-z0-9])?$"
    },
    "inbox": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "schema_version",
        "workspace",
        "inbox_id",
        "capacity_profile_id",
        "status",
        "updated_at"
      ],
      "properties": {
        "schema_version": { "const": 1 },
        "workspace": { "$ref": "#/$defs/workspace" },
        "inbox_id": { "$ref": "#/$defs/canonical_id" },
        "capacity_profile_id": { "$ref": "#/$defs/canonical_id" },
        "status": { "enum": ["enabled", "disabled"] },
        "updated_at": { "type": "string", "format": "date-time" }
      }
    }
  }
}
